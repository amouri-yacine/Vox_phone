{
  "name": "smarthome-pi-bridge",
  "version": "1.0.0",
  "private": true,
  "description": "HTTP → Serial bridge running on the Raspberry Pi, forwarding commands from the Lovable PWA to the ESP8266 IR blaster.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "serialport": "^12.0.0"
  }
}
